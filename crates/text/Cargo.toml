[package]
name = "wc-text"
version.workspace = true
edition.workspace = true
license.workspace = true

description = "Deterministic text cleanup for WhisprCatch — pure functions, no I/O, no network"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
toml = "0.9"
